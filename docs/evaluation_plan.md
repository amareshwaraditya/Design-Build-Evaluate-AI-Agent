# Evaluation Plan

Use the same test dataset across versions. Record answer quality, policy groundedness, tool-selection accuracy, refusal accuracy, escalation correctness, latency, and logging safety. Store input categories and expected outcomes, not customer-identifying data. Use LangSmith traces for live runs and retain deterministic evidence-mode results for repeatability.
